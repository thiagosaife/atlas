Tooltip / field-note card: brightest paper gradient, −0.6° tilt, deep umber shadow, optional uppercase crimson title and italic gold hint footer.

```jsx
<TornNote title="Turkey" hint="click to pin for field notes ✦">
  <EmpireRow name="Ottoman Empire" era="1299–1922 AD" blurb="Six centuries astride three continents." />
</TornNote>
```

Use for hover tooltips and small annotations. Fade+rise it in: `opacity 0→1`, `translateY(6px)→0` over `var(--speed-fast)`.
