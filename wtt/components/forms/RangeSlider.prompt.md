Crimson-accented native range slider — the app's timeline scrubber.

```jsx
<RangeSlider min={-4500} max={2000} step={25} value={year} onChange={setYear} />
```

Pair it with a Cinzel year readout (`fmtYear`: negative years render "500 BC") inside a floating parchment bar.
