Round 30px ghost button holding a single unicode glyph; hairline ink circle, crimson wash on hover. The app's panel-close control.

```jsx
<IconButton title="Close" onClick={close}>✕</IconButton>
```

Use for dismiss/utility actions floating over paper surfaces. Glyphs only — never SVG icons or emoji.
