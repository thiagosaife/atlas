Tiny uppercase crimson kind chip with a hairline crimson border — labels the *type* of a thing in lists ("EMPIRE", "LAND").

```jsx
<Badge>Empire</Badge> <span style={{ fontFamily: "var(--font-display)", fontWeight: 600 }}>Ottoman Empire</span>
```

Square, borderless-fill, never colored backgrounds. Not a status indicator — a taxonomy label.
